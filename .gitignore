# Dependencies
node_modules/

# Build output — produced by CI and by `prepublishOnly`.
# npm packs `files: ["dist"]` from the built output, so it never needs committing.
dist/

# Local scratch
packages/fds-transformer/poc-test/output/

# OS
.DS_Store
