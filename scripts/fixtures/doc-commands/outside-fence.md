# Outside a fence

A command written into a sentence renders as prose, cannot be copied cleanly,
and is invisible to anything that reads fenced blocks. Run
npx --package=ajv-cli ajv validate -s schema.json -d document.json to check a
document, the paragraph says, and nothing has ever run it.
