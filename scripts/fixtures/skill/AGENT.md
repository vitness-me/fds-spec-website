# A method document that has turned into a knowledge document

Every line below is wrong on purpose. Do not fix them; `expected-failures.txt`
records the errors they produce, and repairing one here breaks the self-test.

This is the drift rule 6 exists for. Nothing here is *false* — that is the point.
A procedure that quietly starts restating the manifest reads better than one that
does not, right up to the release that moves a version and leaves this file
saying the old thing with total confidence.

## It states a version

Build against release 1.4.0 unless the caller pinned an older one.

## It names a field the standard defines

Read the prescribed load out of `loadTarget` before you present the set.

## It quotes a published path

Validate the result against
specification/schemas/workout/v1.1.0/workout.schema.json rather than trusting
the transform.
